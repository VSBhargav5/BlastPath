# Contributing

Keep BlastPath a *change* graph, not a Graphify clone.

- Python-first until a second language earns its keep
- Every edge needs provenance
- Tests for parsers, radius, owners, snapshots
- One idea per commit, written as if a reviewer will read it
